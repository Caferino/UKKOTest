<p style="color: green"><%= notice %></p>

<h1>Stores</h1>

<!-- Search Bar -->
<%= search_form_for @q do |f| %>
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 border-bottom">
        <div class="input-group">
            <div class="form-outline">
                <%= f.search_field :codename_cont, class: 'form-control' %>
                <label class="form-label" for="form1">Search</label>
            </div>
            <%= button_tag type: 'submit', class: 'btn btn-primary' do %>
                <i class="fas fa-search"></i>
            <% end %>
        </div>
    </div>
<% end %>


<!-- Original Ruby on Rails Template
<div id="stores">
  <% @stores.each do |store| %>
    <%= render store %>
    <p>
      <%= link_to "Show this store", store %>
    </p>
  <% end %>
</div>

<%= link_to "New store", new_store_path %>
-->

<!-- Stores Cards -->
<div id="container-for-stores" class="">
    <div class="dashboard-content-title">
        <h1> Stores </h1>
    </div>

    <!-- Stock Cards for Stores -->
    <% @stores.each do |store| %>
        <%= render partial: 'store_card', locals: { store: store } %>
    <% end %>

    <br>

</div>
